name: restful_consumer
description: "A new Flutter package project."
version: 1.0.1
publish_to: none

environment:
  sdk: ^3.9.2
  flutter: ">=3.35.4"

dependencies:
  flutter:
    sdk: flutter

  equatable: ^2.0.7
  dio: ^5.9.0
  dio_logger_plus: ^1.2.1
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

